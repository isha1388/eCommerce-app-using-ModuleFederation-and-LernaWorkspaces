import React from 'react';
import IconButton, {IconButtonProps} from '@mui/material/IconButton';

const MuiIconButton = (props: IconButtonProps): JSX.Element => {
  return <IconButton {...props} />
}

export default MuiIconButton;