import React from 'react';
import CardContent, {CardContentProps} from '@mui/material/CardContent';

const MuiCardContent = (props: CardContentProps): JSX.Element => {
  return <CardContent {...props} />
}

export default MuiCardContent;