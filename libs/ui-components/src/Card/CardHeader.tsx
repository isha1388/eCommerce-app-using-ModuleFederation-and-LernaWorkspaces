import React from 'react';
import CardHeader, {CardHeaderProps} from '@mui/material/CardHeader';

const MuiCardHeader = (props: CardHeaderProps): JSX.Element => {
  return <CardHeader {...props} />
}

export default MuiCardHeader;