import React from 'react';
import CardActions, {CardActionsProps} from '@mui/material/CardActions';

const MuiCardActions = (props: CardActionsProps): JSX.Element => {
  return <CardActions {...props} />
}

export default MuiCardActions;