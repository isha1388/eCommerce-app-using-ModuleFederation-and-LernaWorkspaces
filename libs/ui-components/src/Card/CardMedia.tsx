import React from 'react';
import CardMedia, {CardMediaProps} from '@mui/material/CardMedia';

const MuiCardMedia = (props: CardMediaProps): JSX.Element => {
  return <CardMedia {...props} />
}

export default MuiCardMedia;