import React from 'react';
import Typography, {TypographyProps} from '@mui/material/Typography';

const MuiTypography = (props: TypographyProps): JSX.Element => {
  return <Typography {...props} />
}

export default MuiTypography;